import { TabsDirectiveDirective } from './tabs-directive.directive';

describe('TabsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
